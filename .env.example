# Supabase (obrigatório para o app)
VITE_SUPABASE_URL=https://seu-projeto.supabase.co
VITE_SUPABASE_ANON_KEY=sua-anon-key-aqui

# Supabase Service Role (para scripts CLI: criar admin, seed, etc.)
# Obter em: Dashboard → Settings → API → service_role
SUPABASE_SERVICE_ROLE_KEY=sua-service-role-key-aqui

# Supabase DB URL (opcional, para seed/scripts que usam conexão direta)
# Obter em: Dashboard → Settings → Database → Connection string → URI
# SUPABASE_DB_URL=postgresql://postgres.[PROJECT_REF]:[SENHA]@aws-0-[REGIAO].pooler.supabase.com:6543/postgres
